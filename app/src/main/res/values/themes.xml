<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.CampsiteCommander" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Primary brand colors -->
        <item name="android:colorPrimary">@color/primary_red</item>
        <item name="android:colorPrimaryDark">@color/dark_red</item>
        <item name="android:colorAccent">@color/secondary_red</item>
        <!-- Background -->
        <item name="android:windowBackground">@color/background_white</item>
        <item name="android:colorBackground">@color/background_white</item>
        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
    </style>

</resources>
